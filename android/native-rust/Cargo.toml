[package]
name = "p2pshare_android"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = "0.21"
libc = "0.2"
sha2 = "0.10"
zstd = "0.13"

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"
